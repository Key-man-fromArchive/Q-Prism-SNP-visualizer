# P6-S1-T1 — Product name unification: Q-prism® Cluster Caller

Branch: `feedback/p6-brand` (worktree `worktree/feedback-p6`), shared with
concurrent P6-S2-T1 work. Only files in this task's write scope were staged
and committed; P6-S2-T1's in-progress changes (`src/index.css`,
`src/lib/plotly-theme.ts`, `src/lib/constants.ts`, `src/lib/genotype.ts`,
`src/components/protocol/ProtocolTab.tsx` and their tests, plus new
`src/index.css.test.ts` / `src/test/primary-button-contrast.test.ts`) were
left untouched and unstaged.

## Adopted name and casing

Per D-1 (`docs/planning/06-tasks.md`) and the brand guide
(`docs/planning/feedback-2026-09-11/brand-identity-reference.md`, sourced
from `/mnt/docker/asg-saas-v2/docs/brand/Q-PRISM-BRAND-IDENTITY.md`):
**`Q-prism® Cluster Caller`** — lowercase `p`. The guide's "잘못된 표기"
list explicitly marks `Q-Prism` (capital P) as incorrect.

## 1. Product name unified across surfaces

| File | Change |
| --- | --- |
| `snp-analyzer/frontend/src/locales/en.ts:156` | `appTitle`: `'ASG-PCR SNP Discrimination Analyzer'` → `'Q-prism® Cluster Caller'` |
| `snp-analyzer/frontend/src/locales/ko.ts:156` | `appTitle`: `'ASG-PCR SNP 판별 분석기'` → `'Q-prism® Cluster Caller'` (same string in both locales — product name is a proper noun, per assignment) |
| `snp-analyzer/frontend/index.html` | `<title>`, `og:title`, `og:description`, `description`, `keywords` updated to the new name (see below) |
| `README.md:1` | `# Q-Prism® SNP Visualizer` → `# Q-prism® Cluster Caller` |
| `snp-analyzer/app/reporting/pdf_builder.py` | Footer string: `"Report generated by ASG-PCR SNP Discrimination Analyzer | Powered by Invirustech"` → `"Report generated by Q-prism® Cluster Caller | Powered by Invirustech"` |

**No subtitle was added to `appTitle`.** The assignment offered a subtitle
(`SNP 판별 · 대립유전자 클러스터링`) as optional ("필요하면"). `Header.tsx`
renders `appTitle` as a single `<h1>` and is not in this task's write scope
(P6-S3-T1 owns the hero-block/subtitle work per `06-tasks.md`), so adding a
second locale key and a UI element for it would exceed this task's scope
and risk colliding with P6-S3-T1. Deferred to that task.

### `index.html` detail

- `<title>`: `ASG-PCR SNP Discrimination Analyzer` → `Q-prism® Cluster Caller`
- `og:title`: `ASG-PCR SNP Discrimination Analyzer | IVTTools` → `Q-prism® Cluster Caller | IVTTools`
- `og:description` / `description`: prefixed with the product name (e.g.
  `Q-prism® Cluster Caller parses and analyzes raw ASG-PCR ... data ...`)
  so the brand appears in link previews and search snippets, matching the
  "제품명 통일" objective. Neither field named the product before.
- `keywords`: added `Q-prism, Q-prism Cluster Caller` ahead of the existing
  terms (plain text, no `®`, consistent with the other unmarked keyword
  terms in that list).
- `canonical` (`https://snpanalyze.ivttools.com/`): **unchanged**, per
  explicit instruction — SEO impact is a separate decision. Flagged here as
  the "검토 필요" record the assignment asked for; no code change made.

## 2. `Q-Prism` → `Q-prism` casing correction (active surfaces only)

Fixed everywhere it is human-facing descriptive prose in the files listed
below. Left untouched: `docs/planning/archive/**`, `docs/planning/
ui-ux-overhaul/**`, `snp-analyzer/venv/`/`.venv/`, and every filename/path/
identifier (`qprism-*.csv`, `qprism-mark.svg`, `qprism-file-workspace`,
contract ID `qprism-feedback-20260911-v1`, repo directory name/clone URL
`Q-Prism-SNP-visualizer`).

| File | What changed | What was excluded and why |
| --- | --- | --- |
| `snp-analyzer/frontend/src/locales/en.ts` | `importTemplatesDescription`, `templateRdesHelp` (2 strings) | — |
| `snp-analyzer/frontend/src/locales/ko.ts` | `importTemplatesDescription`, `templateRdesHelp` (2 strings) | — |
| `README.md` | 6 prose/table occurrences (overview line, disclaimer, feature bullet, instrument table row, RDES paragraph, parser list, `rdes.py` comment) | Repo clone URL/`cd` paths (`git clone .../Q-Prism-SNP-visualizer.git`, `cd Q-Prism-SNP-visualizer[...]`, lines 73/74/95/102) are the actual GitHub repository name and directory on disk — an identifier, not display text; renaming it here without renaming the repo would make the Quick Start instructions wrong. Template filenames (`qprism-rdes-amplification-template.tsv` etc.) already lowercase, untouched. |
| `docs/planning/06-tasks.md` | Title, D-1/D-2 decision rows, P6-S1-T1 and P6-S2-T1 headings/body text, the `Q-prism 마크` table label, the hero-block sentence | The evidence-path prose lines `` `/mnt/docker/Q-Prism-SNP-visualizer/worktree/feedback-p0/.../python` `` (an actual filesystem path) left as-is. |
| `docs/planning/feedback-2026-09-11/00-overview.md` | D-1/D-2 decision rows | — |
| `docs/planning/feedback-2026-09-11/FB-02-upload-entry-and-brand.md` | Analysis prose (grep-result sentence, wireframe mockup labels, asset table row) | Line 5, the `**원문**:` block — the literal, verbatim user feedback submission (`0de8fb3361884951`) — was **not** touched; it still reads `Qprism` / `Q-Prism 로고` / `Q-Prism art` exactly as the user typed it. Editing a direct quote of what the user actually wrote would misrepresent the historical record. |
| `docs/planning/feedback-2026-09-11/FB-07-identity-and-ia.md` | Requirement table (`e`/`f` rows), analysis prose, decision rows (D-1/D-2), "사용자 제안" line | Lines 10 and 12, inside the `**원문**:` blockquote (feedback `36be23963de2477d`) — the user's actual words ("Q-Prism의 컬러", "Q-Prism(R) Cluster Caller") — were **not** touched, for the same reason. |
| `docs/planning/feedback-2026-09-11/brand-identity-reference.md` | None needed | Its one `Q-Prism` occurrence (`- ❌ Q-Prism`) is the guide's own example of the *incorrect* form — correct as written. |

Explicitly **not** touched anywhere: `docs/planning/archive/**`,
`docs/planning/ui-ux-overhaul/**`, `snp-analyzer/venv/`, any `.venv/`.

### Test fixtures checked and left alone

`snp-analyzer/tests/test_import_models.py` (`instrument="Q-Prism"`, x2) and
`snp-analyzer/tests/test_import_api_p3.py` (`instrument="Q-Prism Synthetic
RDML"`, x2): read both files in full context. These are synthetic
`instrument` metadata values for `ImportRun`/`UploadResponse` fixtures —
arbitrary placeholder strings analogous to `"QuantStudio"` or `"CFX Opus"`
in other tests, not assertions about our product's brand name. They are
not stored keys or filenames. Left unchanged as out of scope for a product
name unification task; changing them would not affect any brand-facing
surface.

## 3. Preserved as instructed

- ASG integration copy (`Save result to ASG Designer`, `backToAsgDesigner`,
  etc.) — unchanged. These name the counterparty system, not our product.
- `canonical` URL — unchanged (see §1).
- README.md:11 disclaimer — casing fixed (`Q-Prism®` → `Q-prism®`) but the
  disclaimer sentence itself ("not affiliated with ... Applied Biosystems,
  Thermo Fisher Scientific, or Bio-Rad Laboratories") is verbatim unchanged.

## 4. Known inconsistency (recorded, not fixed here)

The logo image `snp-analyzer/frontend/public/brand/qprism-wide.png` itself
renders the wordmark as **`Q-Prism®`** (capital P), while all text in this
codebase now reads `Q-prism®` (lowercase p). Re-rendering the logo asset is
out of this contract's scope (owned by `asg-saas-v2`/P6-S3-T1, which is
BLOCKED on D-3 asset delivery). Flagging so it isn't mistaken for a missed
casing fix.

## Follow-up — `AppFooter` still carried the old name (2026-09-12)

Team lead caught a missed surface: `AppFooter.tsx` renders on every screen
(login and post-login) and read `ASG-PCR SNP v1.0.0` — the exact "three
names in one place" defect this task exists to fix, just missed because
the initial write scope did not name this file.

| File | Change |
| --- | --- |
| `snp-analyzer/frontend/src/components/layout/AppFooter.tsx:28` | `` ASG-PCR SNP {…version…} `` → `` Q-prism® {…version…} ``. Shortened from the full `Q-prism® Cluster Caller` because the footer is a single narrow line already sharing space with commit hash and build timestamp; `Q-prism®` alone still identifies the product unambiguously next to the header's full name. The version number logic (`build.version.startsWith("v") ? ... : ...`) is untouched — the `@SPEC` comment's stated purpose (operators must be able to quote a version) is preserved exactly. |
| `snp-analyzer/frontend/src/components/layout/AppFooter.test.tsx:14` | Assertion updated to `'Q-prism® v1.0.0'` — same test, same version value, only the product-name prefix changed. |
| `tests/01-homepage.spec.ts:7` | `toHaveTitle('ASG-PCR SNP Discrimination Analyzer')` → `toHaveTitle('Q-prism® Cluster Caller')` |
| `tests/01-homepage.spec.ts:48` | `/ASG-PCR SNP v\d+\.\d+\.\d+/` → `/Q-prism® v\d+\.\d+\.\d+/` — version-number pattern (`\d+\.\d+\.\d+`) kept exactly as-is, only the literal name prefix changed |
| `tests/01-homepage.spec.ts:74` | `/ASG-PCR SNP (Discrimination Analyzer\|판별 분석기)/` → `/Q-prism® Cluster Caller/` — the en/ko alternation was removable because P6-S1-T1 already made `appTitle` identical in both locales (`Q-prism® Cluster Caller`), not a weakening of the assertion; it still pins the exact rendered header text |

No assertion was weakened or removed; each still requires an exact string
or an exact version-number shape.

### Additional known gap found, left unfixed (out of this follow-up's scope)

`snp-analyzer/app/main.py:130` sets the FastAPI app title
(`title="ASG-PCR SNP Discrimination Analyzer"`), which surfaces in the
auto-generated `/docs` (Swagger UI) and `/redoc` pages — a fourth place the
old name still appears. This file was not in either the original or the
follow-up write scope, so it was left untouched; flagging it for the next
task/decision rather than editing outside the assigned scope.

## Verification (all four, from `worktree/feedback-p6/snp-analyzer/frontend`)

```
$ npx tsc --noEmit                 # 0 errors
$ npm run lint                     # 0 errors, 0 warnings
$ npm run test
 Test Files  115 passed (115)
      Tests  807 passed (807)      # baseline unchanged: 115/807
$ npm run build                    # tsc -b + vite build succeeded
```

No test asserts the literal `appTitle` string or the PDF footer string, so
none needed updating.

Backend (from `worktree/feedback-p6/snp-analyzer`, using the shared
`feedback-p0` venv per the task's verification instructions, since
`app/reporting/pdf_builder.py` was modified):

```
$ /mnt/docker/Q-Prism-SNP-visualizer/worktree/feedback-p0/snp-analyzer/venv/bin/python -m pytest --tb=short -q
816 passed, 2 subtests passed in 156.99s
```

Matches baseline (816 passed + 2 subtests) exactly.

## Files touched (write scope)

- `snp-analyzer/frontend/src/locales/en.ts`
- `snp-analyzer/frontend/src/locales/ko.ts`
- `snp-analyzer/frontend/index.html`
- `README.md`
- `snp-analyzer/app/reporting/pdf_builder.py`
- `docs/planning/06-tasks.md`
- `docs/planning/feedback-2026-09-11/00-overview.md`
- `docs/planning/feedback-2026-09-11/FB-02-upload-entry-and-brand.md`
- `docs/planning/feedback-2026-09-11/FB-07-identity-and-ia.md`
- `docs/planning/feedback-2026-09-11/evidence/P6-S1-T1.md` (this file)

## Follow-up verification (all four, from `worktree/feedback-p6/snp-analyzer/frontend`)

Baseline at follow-up time (P6-S2-T1's commits already landed): **117
files / 819 tests**.

```
$ npx tsc --noEmit                 # 0 errors
$ npm run lint                     # 0 errors, 0 warnings
$ npm run test
 Test Files  117 passed (117)
      Tests  819 passed (819)      # matches baseline exactly
$ npm run build                    # tsc -b + vite build succeeded
```

E2E, against the already-running server on port 8151 (frontend rebuilt
first so its `../app/static-react` output picked up the new footer/title
strings):

```
$ E2E_BASE_URL=http://127.0.0.1:8151 npx playwright test tests/01-homepage.spec.ts --project=chromium --workers=1 --reporter=list
11 passed (18.6s)
```

`test-results/` (gitignored) removed after the run; no production port
(8002) or database touched.

## Follow-up files touched (write scope)

- `snp-analyzer/frontend/src/components/layout/AppFooter.tsx`
- `snp-analyzer/frontend/src/components/layout/AppFooter.test.tsx`
- `tests/01-homepage.spec.ts`

P6-S3-T1's concurrent edits to `src/locales/en.ts`/`ko.ts` (seen as
unstaged modifications during this follow-up) were left untouched and
unstaged, per the write-scope boundary in the follow-up assignment.

No P6-S2-T1 files (`src/index.css`, `src/lib/plotly-theme.ts`,
`src/lib/constants.ts`, `src/lib/genotype.ts`,
`src/components/protocol/ProtocolTab.tsx`, or their tests) were staged or
modified. No `archive/`/`ui-ux-overhaul/` docs, no filename/storage-key/
template-path `qprism` identifiers, no ASG integration copy, and no
`canonical` URL were changed. No main merge, remote push, or branch change.

## Second follow-up — pre-existing lint debt in `pdf_builder.py` (2026-09-12)

`app/reporting/pdf_builder.py` (modified in §1 above) carried two
pre-existing `ruff` `F401` (unused import) findings from the main
baseline, unrelated to this task's own changes:

- `pdf_builder.py:9` — `reportlab.lib.units.cm` imported but unused
- `pdf_builder.py:14` — `app.reporting.charts.GENOTYPE_COLORS` imported
  but unused

Since this contract's BE-LINT gate requires every changed `.py` file to
pass `ruff check`, and removing genuinely unused imports is safe and
reduces the pre-existing baseline (36 → 34 findings), both were removed
from `pdf_builder.py`'s import lines (`mm, cm` → `mm`;
`render_scatter_png, render_plate_png, GENOTYPE_COLORS` →
`render_scatter_png, render_plate_png`).

**Re-export check**: confirmed neither `cm` nor `GENOTYPE_COLORS` is
imported from `app.reporting.pdf_builder` (re-exported) anywhere in
`app/` or `tests/` — `GENOTYPE_COLORS`'s only other references are its own
definition in `app/reporting/charts.py` and an unrelated JS constant of
the same name in `app/static/js/amplification-overlay.js`; neither
symbol was re-imported from `pdf_builder` itself. Removal is safe.

Edited by hand (not `ruff check --fix`) to keep the diff to exactly those
two import lines; `git diff` confirmed no other change. `ruff format` was
**not** run — this file is unformatted on `main` already, and a full
reformat would pollute the diff, per instruction.

### Verification

```
$ /mnt/docker/Q-Prism-SNP-visualizer/worktree/feedback-p0/snp-analyzer/venv/bin/ruff check app/reporting/pdf_builder.py
All checks passed!
$ /mnt/docker/Q-Prism-SNP-visualizer/worktree/feedback-p0/snp-analyzer/venv/bin/python -m pytest --tb=short -q
816 passed, 2 subtests passed in 156.48s
```

Matches baseline (816 passed + 2 subtests) exactly; PDF generation path
(`render_scatter_png`/`render_plate_png` calls, both still imported and
used) is unaffected.

### Files touched (this follow-up)

- `snp-analyzer/app/reporting/pdf_builder.py`
- `docs/planning/feedback-2026-09-11/evidence/P6-S1-T1.md` (this file)

P6-S3-T1's commit `1318b80` was not touched or reverted; only the two
files above were staged and committed.
