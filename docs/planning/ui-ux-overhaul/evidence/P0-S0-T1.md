# P0-S0-T1 — Frontend preflight remediation

Date: 2026-09-07. Branch: `ux-followup/p0-preflight`.
Scope: user-approved prerequisite lint, development dependency, and lifecycle
repairs. This task delivery does not claim the independent P0-S0-V gate passed.

## Delivered changes

- Vitest and its V8 coverage provider upgraded together to 4.1.11; Vite to
  7.3.6 with compatible lockfile resolution. No forced independent peer upgrade.
  Earlier baseline had 13 npm findings; initial coverage addition propagated
  existing Vitest risk into a fourteenth metadata finding. Current audit is clean.
- Typed API/Plotly contracts, captured plot cleanup, preset algorithm validation,
  and project summary adapters are delivered in `b969b5d35f6ad6b2c6b4eefa5ba27ed883674d7d`.
  See [typing slice evidence](P0-S0-T1-typing.md) for its nine regression tests,
  approval boundaries, and new helper's 100% coverage / complexity check.
- AnalysisTab validates popup well types against the existing enum and lists
  all captured store actions in hook dependencies. No algorithm was changed.
- ScatterPlot derives boundary seeds, resets render-owned state by request
  identity, and rejects obsolete/unmounted request completions. Existing ready
  plots remain visible during same-session refreshes. Retry still shows loading.
  Plotly selection payloads and custom data are typed/checked, not `any`.
- Header resets ASG presentation state during guarded identity changes; its
  asynchronous revision guard remains. Session/cycle/ROX/mutation changes do
  not allow old save errors to overwrite the current presentation.
- Quality requests are keyed by session/ROX; clearing a session removes its
  prior errors. Effect events obtain current translated fallback errors without
  reissuing requests on language changes. Cycle playback dependencies are
  explicit, with cadence/wrapping/debounce behavior covered by fake timers.
- React 19 and TypeScript skills informed identity-based state, effect-event
  usage, and boundary typing. No lint suppression, deferred-effect workaround,
  new demo route, scientific threshold, or UI redesign was introduced.

## RED → GREEN evidence

- `QualityTab.test.tsx`: clearing a session originally retained its private
  error (1 failing assertion); keyed request component passes. A separate
  language-only test exposed a redundant second request before the effect-event
  correction (expected 1, got 2); now passes.
- `ScatterPlot.requests.test.tsx`: behavioral tests were replayed against the
  original HEAD component with reversible scoped patches, then the corrected
  file restored. Both old-response and unmount cases failed (HEX overwrote VIC).
  Both now pass; an additional real component test drives typed plot selection,
  marker highlighting, and boundary seed refresh.
- `Header.test.tsx`: four pending-save invalidation regressions pass.
- `CycleControl.test.tsx`: 500ms play/wrap/pause and 150ms slider debounce pass.
- State slice total: 11 tests. These supplement the typing slice's nine tests
  and 53 existing tests; this is not a replacement suite.

## Fresh integration verification

Commands run from `snp-analyzer/frontend` unless noted:

| Check | Evidence |
| --- | --- |
| `npm run test` / instrumented full run | Latest 73 passed, 14 files |
| `npm run lint` | Exit 0, 0 errors/warnings; baseline 32 errors / 14 warnings |
| `npx tsc -b --pretty false` | Exit 0 after all new test typing fixes |
| `npm run build` | Exit 0, 1810 modules, 34.43s; JS chunk ~5.51MB warning remains |
| `npm audit --audit-level=high` | Exit 0, found 0 vulnerabilities |
| `git diff --check` | Exit 0 |
| Independent orchestrator run | 69 tests / 14 files and lint passed at 03:51:07 UTC, before final supplemental tests |

Coverage command: `UX_COVERAGE_DIR=/tmp/qprism-p0-final-fe-coverage npm run test:coverage`.
Latest full report: statements 21.41%, branches 21.49%, functions 20.39%,
lines 21.09% (936/4438). The legacy application-wide deficit is reported, not
claimed to satisfy a 70% whole-application gate.

Changed instrumentable lines for this state slice are **51/61 = 83.61%**.
Measured by intersecting added/new-side lines from `git diff --unified=0`
against baseline `b969b5d` with `DA:line,hits` entries in the V8 `lcov.info`;
type-only/non-instrumented lines are not counted. Per-file counts:
AnalysisTab 0/3, CycleControl no instrumentable added lines, ScatterPlot 40/47,
Header 5/5, QualityTab 6/6. This is line coverage, not branch coverage or
coverage of every historical function in those components. Existing large
component complexity was not claimed to become <=10 through these repairs;
new shared helper complexity is independently checked in the typing evidence.

## Browser verification

- Isolated API: 127.0.0.1:18263, local authentication, generated JWT secret,
  temporary SQLite directory, synthetic account and built-in examples only.
  No operating DB or private instrument file was used.
- Existing harness: `E2E_PORT=15263 E2E_BASE_URL=http://127.0.0.1:15263
  VITE_DEV_API_TARGET=http://127.0.0.1:18263 npm run e2e --
  e2e/p5-scatter-view-controls.spec.ts --project=chromium --workers=1`.
  **14 passed in 50.6s**, including UI authentication, axes, NTC controls,
  selection/edit mode, normalization, origin, box selection, dosage ceiling,
  and marker form declaration.
- Inspected actual 1440px synthetic analysis screenshot:
  `/tmp/qprism-p0-ui-analysis.png`. Header, plot, controls and plate render.
  The known tall layout/QC presentation issues remain later PRD work, not
  silently declared resolved by this prerequisite task.
- Inspected injected quality failure screenshot:
  `/tmp/qprism-p0-ui-quality-error.png`. Visible `Synthetic quality failure`,
  no uncaught browser page errors. Route interception was corrected to the
  actual `/api/data/*/quality` endpoint after two harness-pattern timeouts;
  those timeouts were not counted as product failures or successful checks.
- Chrome-browser skill was inspected but is extension-only, with no extension
  connection tools available. Existing Playwright was used as the fallback;
  Claude CLI version check returned 2.1.261. No user Chrome session was touched.

Build output, auth storage, screenshots, traces, and coverage raw data are local
artifacts, not source changes. Full P0 quality/security/ICV approval remains the
orchestrator's independent gate responsibility; P1 is not started by this task.
