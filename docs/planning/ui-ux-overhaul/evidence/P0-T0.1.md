# P0-T0.1 — Preflight evidence

Date: 2026-09-07. Task preparation completed; **quality/release gates are not PASS**. TDD exemption applies to this configuration/documentation task. No product source or operational data changed.

## Provenance and scope

- Worktree: `worktree/ux-followup-p0`; branch `ux-followup/p0-preflight`.
- Code baseline: `80c2c8a24e51c1707b12d06b07f0bf83eb18090a`.
- PRD SHA-256: `973f3ebcb3ed18abdadfc9930939e0a313298cfdf863e126383b0845d012aad4`.
- Baseline task document SHA-256: `b1873340450cc9da424b666938adf2f372de9099e9ac69e8083832cab2f6f6c9`. The specialist read SHA-256 `e61cb018b280ced880f81c8a60871db40a5e30baaa1c141e5017deda699fd7ef` after orchestrator progress edits. Canonical status/runtime edits were not included in the specialist commit.
- Read AGENTS, task twice, PRD, actual routes/models/persistence; applied verification-before-completion skill. Contract appendix explicitly includes attach-catalog mutation, DB-before-memory publication and preservation of legacy provenance.
- Added pytest-cov 7.1.0, pypdf 6.17.0, radon 6.0.1 as dev dependencies; matching Vitest/coverage-v8 2.1.9; existing openpyxl 3.1.5 inspects XLSX. Python venv bcrypt is 4.0.1. `pip check` exits 0.
- Existing root login helpers already support environment credentials and language-independent form submission, so no unnecessary helper rewrite.

## Fresh baseline results

| Command | Result | Artifact |
| --- | --- | --- |
| BE venv pytest --tb=short -q --cov=app, isolated DB_PATH | exit 0; 454 passed, 2 subtests passed, 3 warnings; 53.59 s | /tmp/qprism-p0-be-test.log |
| BE coverage JSON | 68.313% full-project line coverage | /tmp/qprism-p0-be-coverage.json |
| FE npm run test:coverage (repeated after final config) | exit 0; 6 files / 53 tests pass; full lines 18.01%, branches 72.62%, functions 23.54% | /tmp/qprism-p0-fe-test-final.log |
| FE npm run lint / eslint JSON | exit 1; 32 errors, 14 warnings, all existing product files | /tmp/qprism-p0-eslint.json |
| FE npm run build | exit 0; expected >500 kB bundle warning | /tmp/qprism-p0-fe-build.log |
| FE Playwright --list | 51 tests in 10 files | /tmp/qprism-p0-fe-e2e-list.log |
| Root Playwright --list | 58 tests in 8 files | /tmp/qprism-p0-root-e2e-list.log |
| FE p4-s0-single-marker-default + auth setup | exit 0; 4 passed, 13.4 s | /tmp/qprism-p0-e2e.log |
| radon cc app -j | exit 0; baseline inventory, not new-code complexity approval | /tmp/qprism-p0-complexity.json |

BE test discovery occurred before P0-T0.2's new test file was written; 454 is the unmodified product baseline, not a claim about fixture-agent tests. Full browser suite was listed, not executed. Representative E2E used fresh temporary SQLite, generated credentials/JWT, API 18327 and Vite 15327; owned servers stopped after completion. No port 8002 used. Browser artifacts/auth files remain ignored and are not committed.

Initial coverage output under test-results was picked up by ESLint and introduced three artifact warnings. Corrected reports to ignored node_modules/.cache/ux-coverage, moved those generated artifacts outside the repository and reran ESLint. Final 32 errors/14 warnings are product baseline, not tool-generated noise.

## Existing failure ownership

| Existing file group | Errors | Follow-up owner |
| --- | --- | --- |
| lib/api.ts, types/api.ts | 5 | P1-S0-T1 |
| AnalysisTab.tsx | 1 | P2-S1-T1 |
| ScatterPlot.tsx, AmplificationOverlay.tsx, WellDetailPanel.tsx | 16 | P2-S3-T1 / P4-S1-T1; explicit scope check before repair |
| BatchTab.tsx | 2 | P3-S3-T1 |
| Header.tsx | 2 | P2-S3-T1 / P4-S0-T1 |
| CompareTab.tsx, ProtocolTab.tsx, QualityTab.tsx | 6 | P4-S4-T2 / P4-S4-T1 / P4-S2-T1 |

Errors are existing explicit-any and synchronous-effect-state rules. Warnings additionally concern hook dependencies in CycleControl, StatisticsTab, ImportMappingWizard and UploadZone. Do not disable rules or call FE-CHECK green. P0-S0-V must explicitly determine prerequisite remediation or blocking; this evidence does not authorize skipping a failed gate.

## Dependency security — unresolved

`npm audit --json`: 14 findings (2 critical, 7 high, 4 moderate, 1 low). `npm audit --omit=dev --json`: 4 (3 high, 1 low). Reports: `/tmp/qprism-p0-npm-audit.json`, `/tmp/qprism-p0-npm-audit-prod.json`; audit returns nonzero for vulnerabilities. Critical Vitest 2.1.9 predates this task; newly added matching coverage-v8 inherits its vulnerable dependency. Audit suggestions involve major versions and are not automatically applied. Production-filter high names: vite, nanoid, postcss; low esbuild. Deployment reachability must be reviewed separately from package classification.

`venv/bin/pip-audit -r requirements.txt -r requirements-dev.txt --format=json`: exit 1, four vulnerabilities in two existing runtime dependencies. python-multipart 0.0.29: PYSEC-2026-3037/PYSEC-2026-3036 fixed in 0.0.30, PYSEC-2026-3040 fixed in 0.0.31. ecdsa 0.19.2: PYSEC-2026-1325, no reported fix. Artifact `/tmp/qprism-p0-pip-audit.json`. pip-audit was installed only as an environment diagnostic; no broad runtime dependency upgrade performed.

Security gate remains unresolved, especially inherited critical dev tooling and unfixed runtime advisory. Orchestrator must compare original lockfile audit and assess approved remediation scope; no risk acceptance is implied. New-module line gates (70%) and complexity limit (10) are specified in [coverage manifest](coverage-manifest.md), not misrepresented as achieved by baseline totals.

## Handoff

Use [API contract appendix](../05-contract-appendix.md) for P1. Before P1, resolve P0-S0-V's lint/security disposition and incorporate P0-T0.2 evidence. No result contract implementation, migration, UI repair, main merge, push or external notification occurred in this task.
