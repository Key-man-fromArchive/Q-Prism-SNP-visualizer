# P1-R3-T2 — verified report and ASG snapshots

Baseline: `398d2272ec88ff5ceec1cda40e929b8047c3d17e`. Task source: `docs/planning/06-tasks.md`; contract: appendix section 3. Verification date: 2026-09-07.

## Delivered contract

PDF/XLSX capture the same authorized, detached completed result as CSV. Omitted conditions select stored conditions; explicit mismatches, legacy/incomplete context, stale inputs, replaced revisions and pending analysis retain shared rejection behavior. ASG checks ownership, launch linkage/save scope and existing multi-marker schema-3 restriction before common validation. Launch metadata is copied at acceptance; transport is mocked in tests. Diploid schema 1 and polyploid schema 2 remain compact.

Reports retain whole-run rows, captured calls/confidences/types, missing-read status and marker/ploidy identity. Figures use captured numerical conditions and explicit coordinate basis. PDF adds paired identity/confidence and numeric tables, dynamic 96/384 plate extent, marker/ploidy color legend, full-curve Ct with separate conditions and unavailable-value explanation, and embedded analysis context. XLSX keeps the legacy optional Marker prefix and Called counting policy, numeric negative RFU and typed text cells. Context JSON uses numbered chunks; oversized labels/captions return explicit HTTP 400 rather than truncating.

## RED → GREEN evidence

- Initial 10 PDF/XLSX contracts failed on old success responses/provenance; passed after snapshot adapters.
- ASG explicit-user snapshot test failed before adapter integration, then passed.
- Plate/Ct presence failed before restoration.
- Context chunk/oversized-label tests failed before lossless chunking and explicit rejection.
- Figure basis tests failed before captured-basis axes; Called Empty/Omit policy and derived-caption overflow each failed before corrections.
- Focused report suite: 32 passed before metadata-isolation fixture; the final full suite below includes that fixture. It restores sample/protocol/ASG dictionaries with monkeypatch, avoiding cross-test metadata leakage.
- Additional checks cover paired PDF well/call/confidence, pre-read plus reference normalization yielding 8.0, mixed ploidies, missing rows, negative XLSX numeric cells, chart failure propagation, ASG no-post rejection states, schema 2, explicit Unknown and launch-copy isolation.

## Final verification

Only the final frozen-source execution counts; superseded runs were terminated and excluded.

Command (from `snp-analyzer`, using its venv):

```sh
UX_REPORT_ARTIFACTS=/tmp/qprism-p1-reports-render venv/bin/python -m pytest -q --tb=short --cov=app --cov-report=json:/tmp/qprism-p1-reports-final-coverage.json
```

Result: **710 passed, 33 warnings, 2 subtests passed**, exit 0, 177.73 seconds. Full output: `/tmp/qprism-p1-reports-final.log`. Warnings are visible in that log; no failure was waived.

Coverage intersects added lines from `git diff 398d227 --unified=0` with coverage.py executed/missing executable lines. New modules use whole-module coverage.

| Module | Covered added executable lines |
| --- | --- |
| app/asg_result.py | 42/45 (93.3%) |
| app/reporting/charts.py | 3/3 (100%) |
| app/routers/asg.py | 5/5 (100%) |
| app/routers/data.py | 4/4 (100%) |
| app/routers/export.py | 3/3 (100%) |
| snapshot_pdf.py (new) | 71/72 (98.6%) |
| snapshot_plate.py (new) | 20/20 (100%) |
| snapshot_presentation.py (new) | 40/40 (100%) |
| snapshot_xlsx.py (new) | 66/67 (98.5%) |

Scoped `ruff check` passed for all modified/new production files. `mypy --follow-imports=silent --ignore-missing-imports` passed for the eight adapter/router modules; the existing chart module is checked by Ruff and execution tests. `radon cc` reports every new logical function at most 10 (XLSX builder 10; others at most 8). Existing untouched chart/builder logic is not represented as newly compliant. `git diff --check` passed.

## Rendering and dependency evidence

The final raster test extracts exact long Korean text, asserts an embedded TrueType font and rasterizes **all 21 pages** using pypdfium2. Artifacts: `/tmp/qprism-p1-reports-render/korean-report.pdf` and `korean-page-1.png` through `korean-page-21.png`. Parent reviewed all original table pages and re-reviewed changed pages 1/2/15/16/21: glyphs, full wrapped/escaped labels, repeated headers, numeric tables, raw axes, legend and Ct explanation are readable without clipping. Implementer also inspected actual scatter/legend images. No signature-only PDF claim is used. The 384 extent is additionally asserted against real matplotlib axes.

`pypdfium2==5.13.0` is development-only, installed from a prebuilt wheel. Original venv bootstrap pip 24.0 audit findings were remedied by approved venv-only pip 26.2 upgrade; no host package change or runtime dependency addition. Final `pip check`: no broken requirements. Final `pip-audit --progress-spinner off`: no known vulnerabilities.

Unmodified NanumGothic-Regular.ttf and OFL license are bundled under `app/reporting/fonts/`, with upstream commit and SHA-256 in that directory's README. Both checksums were verified. Staged diff checking reports the upstream license's two trailing-space lines (2 and 24); these are preserved deliberately to keep the licensed upstream artifact byte-identical. Authored files pass whitespace checks. Font lookup is repository-relative; PDF generation does not depend on host fonts or viewer CJK fallback. Python/PDF/verification skills guided evidence-first checks; fixed scientific XLSX values deliberately remain immutable rather than adding recalculating formulas.

## Boundaries

No scientific algorithm/threshold changes, external ASG posts, operational DB access, servers, host installs, merge or push. Existing legacy report-builder APIs remain available; production endpoints now use snapshot adapters. Formula-like text is preserved as typed XLSX strings; this does not assert universal spreadsheet safety after third-party conversion. User labels beyond XLSX's cell limit are explicitly rejected, with CSV suggested for full text.
