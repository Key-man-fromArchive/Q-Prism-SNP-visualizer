# P2-S1-T1 — Analysis state integration

## Outcome

PASS, 2026-09-07. Baseline `83887a4`; branch `ux-followup/p2-result-ui`. Existing AGENTS.md preserved. No scientific algorithm, dependency, deployment or URL-history changes. Work was inherited from the previous implementation agent and completed without resetting its changes.

## Implemented contracts

- Shared owner/session/request tickets control all analysis publication; a separate load generation preserves initialization. Metadata revision merges monotonically, and refresh failure marks input revision unknown without discarding the last completed result.
- Single analysis is explicit except the one-shot fresh-upload initialization. Reopened sessions do not auto-run. Multi-marker inputs settle for 220 ms; playback and restoration have separate suppression rules. Actual acquisition cycles and sparse/zero coordinates drive the slider and accessible labels.
- Current-view request ownership is separate from submitted and completed snapshots. Surface registration and control patches preserve explicit manual settings; disabling boundary mode clears manual cuts. Result projection is the sole assignment publisher. Failed boundary/dosage actions do not overwrite accepted metadata.
- KO/EN status distinguishes pending, latest failure, retained cycle, missing/legacy/incomplete provenance, unconfirmed/stale input and current-view mismatch. Tabs/panels and sparse-cycle controls have explicit accessible associations.
- Settings cannot supersede a restoring load. Scatter and plate responses are discarded after ownership/entry changes or cleanup. Auth changes synchronously invalidate analysis and navigation state.
- Optional backend `cycle_mode=absolute` selects literal zero; omitted mode retains legacy zero/latest semantics. React selected-cycle scatter/plate/analysis requests opt in, and status comparison uses the same explicit mode. QC and export/ASG mode plumbing remain documented P2-S2/P2-S3 follow-ups; omitted captured-cycle export remains safe.

## Regression evidence

RED→GREEN cases include malformed/empty cycle metadata, failed revision refresh, rejected dosage/boundary edits, playback cadence, restoring Settings action, detached multi/plate responses, and actual-zero interpretation. Sparse `[0,10,40]` backend tests initially failed in all three absolute routes (3 failed/6 passed), then passed. Status comparison separately reproduced both wrong 0/0 mismatch and wrong 0/40 match before repair.

## Final verification

- FE full coverage: **245 passed, 39 files, 5.95 s**. `npm run test:coverage -- --coverage.reportsDirectory=/tmp/qprism-p2-takeover-coverage-final`.
- ESLint PASS; final command excludes generated `test-results/**` because concurrent browser artifact cleanup caused an earlier directory-enumeration ENOENT. No source rules disabled for this repair. `npm run build` includes TypeScript: PASS, Vite 39.84 s; existing large-bundle warning remains.
- New logical units checked with ESLint complexity ≤10. Backend cycle resolver Radon CC4. Scoped Ruff PASS and mypy PASS (existing sklearn import-untyped excluded; legacy untyped-body notes retained).
- BE `test_cycle_selection.py`, `test_session_cycles.py`, `test_analysis_revision_races.py`: **35 passed, 3 deprecation warnings, 18.35 s**. Approved isolated P1 venv interpreter executed P2 source; no host install. Full BE revalidation belongs to the P2 phase gate.
- Independent source reviewer: PASS after absolute-mode status correction. Parent-reported actual Chromium verification: P5 **14/14 PASS**, targeted P2 smoke PASS; parent inspected screenshots. Browser artifacts retained in `/tmp` by the gate owner.

## Coverage

Final LCOV: `/tmp/qprism-p2-takeover-coverage-final/lcov.info`; mapping script `/tmp/qprism-p2-line-gate.cjs` intersects added lines against baseline with unique aggregated DA entries. Every modified production module exceeds 70%; minimum is MultiMarker 25/30 (83.3%). New modules: workspace hook52/52, current-request hook18/18, scheduler9/10, actions21/22, session10/10, projection10/10, status31/32. Modified context5/5; PlateView9/9; Scatter14/15; Settings17/17; CycleControl35/36; AnalysisTab15/17. Other changed modules also meet the gate. Whole legacy FE coverage is separately **2081/4684 lines (44.42%)**, not claimed as a whole-project 70% pass.

BE JSON `/tmp/qprism-p2-cycle.json`: new resolver100%; changed executable model1/1, clustering2/2, data3/3. Session response field has no independent executable line; its route is tested by four cycle/authorization cases.

React/TypeScript/Python skills guided typed ownership and narrow helpers; verification-before-completion required fresh metrics and independent review before completion. No merge or push performed.
