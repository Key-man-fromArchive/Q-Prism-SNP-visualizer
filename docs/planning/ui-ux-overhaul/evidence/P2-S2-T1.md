# P2-S2-T1 — Result-aware QC

## Outcome

PASS, 2026-09-07, baseline `8c6c08d`. Independent source review and parent-owned browser gate accepted. Existing AGENTS.md preserved. No algorithm, thresholds, dependencies, operating data or deployment changes.

## Implemented contracts

- QC queries are keyed by owner/session/entry, current input and result revisions, local analysis lifecycle and current cycle/reference/background settings. Old responses cannot publish after query/owner changes. Explicit retry remounts the consumer; a mount-local promise prevents duplicate StrictMode requests without cross-owner caching.
- Compact call rate remains visible. Details distinguish verified/stale/legacy/missing stored judgments, local pending/failed state and revision uncertainty. Stored conditions and current plate-wide NTC conditions show separate captured/requested/applied normalization bases.
- NTC normal/warning/none/insufficient are distinct. Only `flagged=true` enters the warning list; nullable signals/flags remain unavailable, never invented zero. Selected marker metrics are authoritative; unselected markers show individual summaries, not pooled separation.
- Full-curve onset appears only after an explicit recommendation, with detected/not-detected/not-evaluated states, literal zero, reason and captured revision. Owner/request/input changes invalidate it. `no_ntc` describes unidentified NTC curves, not absence of manually declared wells.
- QC supports opt-in absolute cycle coordinates, including zero and sparse `[0,10,40]`. Omitted mode retains legacy zero/latest behavior; selected frontend cycles opt in.

## Verification

RED→GREEN regressions covered null/flag distinctions, revision refresh, local pending while recommendation awaits, revision uncertainty, late owner/cycle responses, retained compact call rate, onset invalidation during await/result publication, requested-versus-applied reference basis, KO/EN onset wording, and StrictMode duplicate requests (2 calls before repair; exactly 1 initial plus 1 refresh afterward).

- Fresh FE full coverage: **274 tests / 41 files PASS**, `UX_COVERAGE_DIR=/tmp/qprism-p2-s2-final-fe npm run test:coverage`; log `/tmp/qprism-p2-s2-final-fe.log`.
- `npm run lint`, `npx tsc --noEmit`, `npm run build`: PASS after final hook repair. Existing large-bundle warning remains. Scoped ESLint complexity ≤10 for QC helpers, actions and analysis store: PASS.
- BE `test_qc_absolute_cycle.py` + `test_qc_status_contract.py`: **54 PASS**, 3 existing deprecation warnings, 26.14 s. Scoped Ruff and mypy PASS (existing import-untyped exclusion and untyped-body notes retained). Approved P1 venv ran P2 source; no host installation. Artifacts `/tmp/qprism-p2-s2-be.{json,log,coverage}`. Full BE revalidation remains the P2 phase gate's responsibility.
- Parent-run ROOT `tests/19-qc-status.spec.ts`: **2/2 PASS**, KO+EN, 12.0 s. Final external Chromium smoke confirmed one settled refresh request, rejected late responses, local pending precedence, independent revision uncertainty, and marker summaries excluding pooled999.00. Parent inspected `/tmp/qprism-p2-browser-qc-{late,pending,input-unknown,markers}.png`. P5's prior14/14 belongs to S1, not this task. Browser Vite was stopped; API18264 remains parent-owned.

## Coverage and review limits

Added executable lines versus baseline, or entire new module: QcBadges15/15; analysis-actions2/2; api1/1; analysis-store7/7; en12/12; ko11/12 (91.7%, captured-label function not exercised in KO); QcDetails26/27 (warnings rendering uncovered); QcOnset6/6; use-qc-response18/18. Every module exceeds70%. QC backend added executable import/resolver lines are covered; whole existing QC module233/241 (96.68%). Existing whole-project frontend coverage is not claimed as a70% pass. Backend reuses the existing bounded resolver and removes a legacy conditional; no new algorithmic decision graph.

Independent reviewer confirmed ownership, StrictMode cleanup, retry and corrected onset wording. Parent inspected browser screenshots and accepted the gate. Browser onset text assertion was prevented by details remount; source/unit tests prove the revised label and explicit-only lifetime. Absolute QC zero is proven by backend/API/unit tests; prior S1 browser evidence proved the absolute serialization pattern. These are explicit layer-specific limitations, not claims that the browser exercised every synthetic edge case.

React/TypeScript skills guided keyed ownership and typed state; verification required fresh regressions and independent review. No merge or push performed; P2-S3 was not started.
