# P3-S3-T1 — Explicit recovery and upload outcomes

Baseline: `b1a4617`. Scope: frontend recovery only; no backend, dependency, scientific algorithm, taskbook, or deployment changes.

## Implementation and RED-first evidence

- Preset list/save/delete have independent operation states. A confirmed save followed by failed GET remains saved; retry refresh performs GET only. Form input/selection survive failed mutations. Malformed successful list payloads are rejected before rendering.
- Recent and Project session recovery share validated, latest-request/owner/auth-generation/entry-pinned reads and opens. Empty, pending and failed lists differ; fixed localized notices replace private transport details. A successful refresh alone clears Project selection.
- Memory-only upload jobs retain filename, stage, fixed reason and confirmed SID, never File/blob/bytes/mapping/token/raw payload. Auth reset/user replacement clears the store; component/tab remount does not. Reload recreates memory. No automatic retry or partial-batch navigation; Project inspection is explicit.
- Raw, mapped and XML paths retain known failures versus response-lost/invalid-success `unknown`. XML source names share one archive outcome. Packaging and directory traversal check ownership before/after asynchronous reads and before dispatch. Example/preview/import callbacks cannot revive obsolete sessions; timers removed. XLSX preview fallback requires HTTP 400 with the existing parser-failure prefix, not auth/network/server failures.
- Demonstrated failures before fixes: malformed recent elements (3), delayed example logout, preview logout, held XML packaging owner switch, format-only fallback, mapped secret/late owner (2), failed XML metadata, directory error callback, and Project raw-detail/late logout (2). Two-source XML success asserts exactly one archive POST and two metadata-only records.

## Fresh verification

- `UX_COVERAGE_DIR=/tmp/qprism-p3-s3-final-coverage npm run test:coverage`: **452 tests / 72 files passed**; whole FE line coverage **67.08% (3618/5393)** (legacy baseline, not the changed-code gate).
- `npm run lint`, `npx tsc -b`, `npm run build`: PASS; build 32.37s. Existing large Plotly chunk warning remains.
- `npm audit --json`: zero findings. `git diff --check`: PASS.
- Root `tests/22-error-recovery.spec.ts`, Chromium, one worker: 3 scenarios pass. Real isolated authentication/example/session APIs; route-injected preset, upload and recovery failures. Covers list 500→empty retry, open 403/404/500→401 login; save failure/input retention/success-with-refresh-failure; partial success/failure/network-unknown, visible explicit Project navigation, tab retention and logout clearing, no repeated upload POST.
- Backend source/tests unchanged; full backend regression remains the P3 phase gate, not claimed rerun here.

## Coverage and complexity

Measured LCOV executable `DA` rows intersected with added lines from `git diff b1a4617 --unified=0`; new modules measured whole. Exact missing lines and current/baseline ESLint complexity are in `/tmp/qprism-p3-s3-final-metrics.txt`.

| Module | Covered / executable |
| --- | --- |
| PresetFeedback | 5/5 |
| RecoveryNotice | 4/5 |
| RecentSessions | 5/7 |
| SessionRecoveryFeedback | 5/6 |
| UploadJobSummary | 7/7 |
| use-owned-operation | 13/13 |
| use-preset-operations | 49/50 |
| use-recent-sessions | 29/29 |
| import-job | 24/26 |
| recovery-payload / recovery-reason | 6/6; 3/3 |
| upload-jobs / upload-response | 30/30; 9/10 |
| xml-upload / upload-job-store | 35/35; 14/14 |
| BatchTab / SettingsTab added lines | 5/5; 3/3 |
| ImportMappingWizard / UploadZone added lines | 19/23; 58/73 |
| auth-store added lines | 1/1 |

All gates exceed 70%; new functions are at most CC10. Unchanged legacy units remain explicitly separate: Batch root25/callback15, Settings root13, Mapping root43/helpers40/11/12/17. UploadZone root decreased14→13; expanded asynchronous handlers were extracted below10. KO/EN additions are literal translations with no added executable DA rows.

## Runtime, visual inspection and limitations

Isolated P3 source used the approved P1 venv interpreter, loopback API18265/Vite15265, and fresh SQLite under `/tmp/qprism-p3-recovery-runtime-SXAS1W`; credentials stayed outside the repository. No real uploads/operating database or ASG posts. Browser artifacts are under its `results/` and `html/` directories. Upload summary and preset screenshots were visually inspected: localized readable states, separate success/failure/unknown, clear manual navigation and no private detail. Existing dense Header logout wrapping is outside this task; no claim of broader layout acceptance.

Existing backend presets are a global JSON file, not per-user storage; `_load_presets` suppresses malformed/unreadable-file errors. Frontend recovery cannot detect that hidden server failure or provide tenant isolation. This known server-storage caveat is not repaired or represented as fixed by these changes. Native browser file retry/server cancellation/job persistence APIs were not added.

Final root22 rerun: **3 passed, 9.4s**. Explicit `recent-reauth.png` captures the visible login form after the 401 transition; automatic end-of-test screenshots are not used as sole evidence. Final explicit preset/upload/reauth images were inspected. Independent `/root/p2_browser_review` final source/spec/security review: **PASS**, with all prior Important findings resolved. Owned backend PID3907911 and Vite PID3908070 were identity-checked against this worktree and terminated; artifacts retained outside the repository. No deployment or remote push.
