# P4-S1-T1 — Result-first analysis workspace

Baseline `b774c26`. Layout/presentation only; scientific algorithms, mutation/history ownership, analysis request scheduling and export contracts are unchanged.

## Implemented behavior

- Single and multi-marker layouts retain one scatter DOM instance on the left and independently stacked plate/selected-well summary on the right at the existing 1280px breakpoint. Smaller screens preserve source-order sequential review. Detailed results and amplification remain below the primary area.
- Native advanced-settings disclosures default closed. Their visible summary derives axis mode, channels, requested/effective normalization, background, inferred/explicit NTC thresholds and aspect lock from existing state/props. Threshold display uses the existing rounding helper. The dosage Apply draft is unchanged; opening/closing never applies scientific settings.
- Selected address, sample, effective call and confidence remain visible. Missing sample/confidence displays an explicit dash. Detailed manual/automatic call information, numeric readings and amplification are available in a native disclosure. Opening the retained amplification DOM requests Plotly autosize without another data request.
- Scope counts/status remain visible; the longer counting explanation is expandable. Desktop selection controls are compact. Full 96-well cells remain visible; 384 plate, result table, marker list and multiple warnings use bounded internal scroll. Multi-marker sidebar does not stretch to the height of secondary results.

## RED-first and regression findings

The initial 1440×1000 real example failed because a primary element ended at y1557. Further bounds assertions caught the selected summary below the viewport, preventing a cosmetic pass on the plot alone. Final proof selects populated A1 by keyboard and asserts sample, call and confidence alongside all primary bounds ≤1000 and scrollY=0. It waits for actual plot points and completed current-condition status before selection, avoiding initial asynchronous publication timing.

NTC/aspect summary tests reproduced RED2, then passed in KO/EN for inferred/unlocked and explicit/locked states. Disclosure interaction verifies retained chart DOM, autosize, no duplicate amplification request, window resizing and successful PNG download with zero analysis POSTs. No mouse-only replacement weakened the keyboard selection assertion.

P5 initially had four hidden-control failures because nested example6 setup reloaded after the outer disclosure-opening setup. Both nested loads now explicitly open settings in the test. Product defaults remain collapsed and all dosage/Apply assertions are retained.

## Verification and measurement

- FE full suite before final presentation-only refinements: **491 passed**, lint/TypeScript/build passed (32.55s).
- Added executable coverage versus baseline: ScatterViewControls1/1, WellDetailPanel1/1, EN2/2, KO2/2. Other changed lines are non-instrumented JSX/CSS; browser DOM assertions verify them. Whole FE line coverage at that checkpoint: 3746/5439 (68.87%), not a whole-app 70% claim.
- All new functions ≤10 cyclomatic complexity. Existing roots/effects in AnalysisTab, ScatterPlot, MarkerScatterPlot, MultiMarkerAnalysisPanel, PlateView, ScatterViewControls and WellDetailPanel retain exactly their baseline complexity; no suppression/refactor of unrelated algorithms.
- Root24 plus preserved root18/root20/root21/root23: **30 passed (2.4m)**. This covers all five widths × KO/EN × light/dark, exact column breakpoint, linked context, keyboard/axe, export consistency, restore, undo and failure/conflict retention.
- Added multi-marker/384 and populated96 focused cases: **2 passed (12.9s)**. Four markers are created through real UI; long name, 40 warnings and 384 plate/scatter geometry are explicitly presentation-only transport fixtures. Stored calculations/context are not synthesized or recomputed by the layout test. Scoped A1 selection updates the summary; internal scrolling and bounded document width are asserted.
- Root and frontend dependency audits: zero vulnerabilities. Authored diff whitespace clean.

## Visual review and boundaries

Inspected `s1-final-focus-results/.../selected-result-first.png`: complete 96 grid, readable scatter, A1/d2/2/Allele1/100% compact summary and explicit detail path fit the 1440×1000 initial viewport. Multi-marker screenshot verifies readable long context and distinct bounded warning/plate areas. Strict no-extra-page-scroll acceptance applies to the canonical 96-well normal case; secondary data remains reachable below, and 384/multiple-warning cases use internal scrolling.

Artifacts under `/tmp/qprism-p4-header-Mn1Pjv/` and `/tmp/qprism-p4-s1-frozen-coverage/`. Approved repository Playwright fallback used; isolated SQLite/private credentials remain outside the repository.

## Final rerun

- P5 plus existing per-marker analysis suite: **21 passed (1.2m)** after nested disclosure setup correction; `s1-p5-final.log`. This includes selection, axes, NTC editing, normalization, dosage draft/Apply/reset and marker switching/playback.
- Explicit 384 vertical-overflow assertion exposed that 28rem allowed this small-text fixture to fit. The secondary viewport is now 24rem, retaining all rows and existing horizontal scrolling. Final affected 96/multi384/mobile384 subset: **3 passed (19.0s)**; `s1-verified-focus.log`. Internal X/Y scrolling, region focus and document bounds are all asserted.
- Final FE **491 passed**, lint/TypeScript/build pass (**32.15s**); `s1-verified-{fe,lint,build}.log`. Added executable coverage remains **6/6 (100%)** across the four executable modules above. Complexity unchanged by the final CSS/class refinements.
- Both owned runtime processes were identity-checked and stopped; ports 18267/15267 have no listeners. No generated browser artifacts, authentication files or private data are staged.
- Independent final source review: **PASS**, confirmed by the orchestrator after final build. No taskbook or orchestration-state changes are included.
