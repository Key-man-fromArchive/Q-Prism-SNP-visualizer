# .pcrd file decryption key (required for Bio-Rad .pcrd file support)
# Obtain this from your Bio-Rad CFX software installation.
# Without this key, .pcrd uploads will return an error.
PCRD_PASSWORD=
