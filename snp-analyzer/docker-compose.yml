services:
  snp-analyzer:
    build: .
    ports:
      - "8002:8000"
    volumes:
      - ./app:/app/app
      - snp-data:/app/data
      - ./pcrd-pw.txt:/app/secrets/pcrd-pw.txt:ro
    env_file:
      - .env
    environment:
      - PYTHONDONTWRITEBYTECODE=1

volumes:
  snp-data:
